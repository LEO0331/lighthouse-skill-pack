# Static HTML Example

Use `skills/lighthouse-static-html/SKILL.md` when the project is server-rendered HTML/CSS/JS without SPA hydration.

Typical fixes:

- preload hero image
- set explicit image width/height
- inline small critical CSS
- defer non-critical scripts
