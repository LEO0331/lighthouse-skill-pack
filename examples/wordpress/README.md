# WordPress Example

Use `skills/lighthouse-wordpress/SKILL.md` for WordPress themes and plugin-heavy sites.

Typical fixes:

- remove redundant plugin payloads
- delay non-critical plugin assets
- configure page cache + CDN
- optimize media generation workflow
