# React + Vite Example

Use `skills/lighthouse-react-vite/SKILL.md` for React apps built with Vite.

Typical fixes:

- route-level lazy loading
- reduce vendor chunk size
- remove unused client-side packages
- avoid hydration-heavy above-the-fold components
