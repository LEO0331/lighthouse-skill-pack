# Next.js Example

Use `skills/lighthouse-nextjs/SKILL.md` for Next.js applications.

Typical fixes:

- replace `<img>` with `next/image`
- apply `next/font` to reduce blocking
- set script loading strategy
- move logic from client components to server components where feasible
